# 时序数据可视化增强计划

## 📋 问题分析

### 当前问题（适用于所有时序数据）

1. **缺少原始数据叠加** 
   - 当前的 `attention_heatmap.png` 只是单纯的注意力矩阵热力图
   - 没有将注意力权重叠加到原始**时序信号**上（ECG、NLP 嵌入、股票价格等）
   - 无法直观看出模型关注哪些时间步/位置

2. **缺少关键位置标注**
   - **通用时序**: 没有自动检测并标注峰值、谷值、突变点等统计特征

3. **可视化效果差**
   - 使用注意力矩阵的对角线均值，信息损失严重
   - 没有利用梯度信息进行融合
   - 颜色映射方案不专业

---

## 🎯 修改目标

### 核心理念
**设计通用的时序数据可视化工具，支持 ECG、NLP、金融、传感器等多种 1D 序列数据。**

#### 适用范围
- ✅ **通用 1D 时序数据** - 任何单通道或多通道的序列数据
- ✅ **不支持领域专用标注** - 如 ECG 的 P/QRS/T 波、NLP 的词性标注等

#### 预期输出（仅通用图表）

每个样本输出以下图表：

1. **时序信号 + 注意力热力图叠加**（主要图表）⭐
   - 文件名：`sequence_attention_overlay.png`
   
2. **多层注意力对比**（所有 Transformer 层）
   - 文件名：`multi_layer_attention.png`
   
3. **关键时间步标注图**（自动检测峰值/谷值）
   - 文件名：`key_segments_annotation.png`

**说明**: 不再提供领域专用图表（如 ECG 波形标注、NLP 词性标注等）。

---

## 📝 实施步骤

### Step 1: 新增时序数据通用可视化工具类

**文件**: `visualization/timeseries_visualizer.py`

```
"""
时序数据通用可视化工具

支持任何 1D 序列数据的注意力叠加和关键点标注。

设计原则：
- 通用方法：只使用统计学检测，不依赖领域知识
- 职责单一：只做可视化和基础信号处理
- 易于使用：开箱即用，无需配置

使用示例：
    ts_vis = TimeSeriesVisualizer(sampling_rate=500)
    
    # 渲染注意力叠加图
    fig = ts_vis.render_sequence_with_attention(
        sequence=data,              # (L,) 或 (C, L)
        attention_weights=importance,  # (L,)
        save_path="output.png",
    )
    
    # 自动检测并标注峰值
    key_positions = ts_vis.detect_key_positions(data, method='peaks')
    # key_positions = {
    #     "peaks": [(start, end), ...],
    #     "valleys": [(start, end), ...],
    # }
"""

from typing import Dict, List, Optional, Tuple
import torch
from torch import Tensor
import numpy as np
import matplotlib.pyplot as plt
from matplotlib.figure import Figure
from matplotlib.axes import Axes
from scipy.signal import find_peaks


class TimeSeriesVisualizer:
    """时序数据通用可视化器"""
    
    def __init__(
        self,
        sampling_rate: Optional[float] = None,
        time_unit: str = "steps",
    ):
        """
        Args:
            sampling_rate: 采样率 (Hz)，None 表示用时间步索引
            time_unit: X 轴单位 ('seconds', 'milliseconds', 'steps')
        """
        self.sampling_rate = sampling_rate
        self.time_unit = time_unit
    
    @staticmethod
    def detect_peaks(
        signal: np.ndarray,
        distance: int = 50,
        prominence: float = 0.1,
    ) -> List[Tuple[int, int]]:
        """
        检测序列中的峰值区域（通用方法）
        
        Args:
            signal: 输入信号 (L,)
            distance: 峰值间最小距离（点数）
            prominence: 峰值突出度阈值
        
        Returns:
            [(start_idx, end_idx), ...] - 每个峰值区域的起止索引
        """
        peaks, properties = find_peaks(signal, distance=distance, prominence=prominence)
        
        peak_regions = []
        for peak in peaks:
            # 峰值前后各扩展 10 个点作为区域
            start = max(0, peak - 10)
            end = min(len(signal), peak + 10)
            peak_regions.append((start, end))
        
        return peak_regions
    
    @staticmethod
    def detect_valleys(
        signal: np.ndarray,
        distance: int = 50,
        prominence: float = 0.1,
    ) -> List[Tuple[int, int]]:
        """检测谷值区域（对负信号使用 find_peaks）"""
        return TimeSeriesVisualizer.detect_peaks(-signal, distance, prominence)
    
    @staticmethod
    def detect_change_points(
        signal: np.ndarray,
        threshold_factor: float = 2.0,
    ) -> List[Tuple[int, int]]:
        """
        检测序列中的突变点（通用方法）
        
        Args:
            signal: 输入信号 (L,)
            threshold_factor: 差分阈值因子（均值 + factor*标准差）
        
        Returns:
            [(start_idx, end_idx), ...] - 每个突变点的邻域
        """
        diff = np.abs(np.diff(signal))
        threshold = np.mean(diff) + threshold_factor * np.std(diff)
        change_points = np.where(diff > threshold)[0]
        
        change_regions = []
        for cp in change_points:
            start = max(0, cp - 5)
            end = min(len(signal), cp + 5)
            change_regions.append((start, end))
        
        return change_regions
    
    def detect_key_positions(
        self,
        signal: np.ndarray,
        method: str = "auto",
    ) -> Dict[str, List[Tuple[int, int]]]:
        """
        自动检测关键位置（通用方法）
        
        Args:
            signal: 输入信号 (L,)
            method: 检测方法
                - 'peaks': 只检测峰值
                - 'valleys': 只检测谷值
                - 'changes': 只检测突变点
                - 'all': 检测所有类型
                - 'auto': 自动选择（默认同'all'）
        
        Returns:
            {
                "peaks": [(start, end), ...],
                "valleys": [(start, end), ...],
                "change_points": [(start, end), ...],
            }
        """
        result = {}
        
        if method in ["peaks", "all", "auto"]:
            result["peaks"] = self.detect_peaks(signal)
        
        if method in ["valleys", "all", "auto"]:
            result["valleys"] = self.detect_valleys(signal)
        
        if method in ["changes", "all", "auto"]:
            result["change_points"] = self.detect_change_points(signal)
        
        return result
```

---

### Step 2: 实现注意力 - 梯度融合并生成可视化

**目标**: 将注意力矩阵和梯度信息融合，生成每个时间点/token 的重要性得分，并输出可视化图表。

#### 2.1 新增核心函数

**文件**: `analyzer/fusion_utils.py` (已有文件，需增强)

```python
"""融合工具函数 - 注意力与梯度的融合"""

import torch
from torch import Tensor
from typing import Dict, Optional


# ========== 已有的归一化和融合函数 ==========
def normalize_for_fusion(tensor: Tensor, ...) -> Tensor:
    """百分位裁剪 + Min-Max 归一化至 [0, 1]。"""
    # ... 已有实现 ...

def gradcam_fusion(attention: Tensor, gradient: Tensor) -> Tensor:
    """Grad-CAM 式融合：normalize(attention * gradient)。"""
    # ... 已有实现 ...

def weighted_sum_fusion(attention: Tensor, gradient: Tensor, alpha: float = 0.5) -> Tensor:
    """加权求和融合：alpha * attention + (1-alpha) * gradient。"""
    # ... 已有实现 ...


# ========== 新增：Token 重要性计算 ==========
def compute_token_importance(
    attention_maps: Dict[int, Tensor],   # {layer_idx: (B, H, L, L)}
    hidden_gradients: Dict[int, Tensor], # {layer_idx: (B, L)}
    method: str = "gradcam",             # 'gradcam' | 'weighted_sum' | 'multiply'
    layer_weights: Optional[Dict[int, float]] = None,  # 各层权重（默认均匀）
) -> Tensor:
    """
    计算每个 token 的重要性得分（用于 ECG 的每个时间点/NLP 的每个 token）
    
    Args:
        attention_maps: 各层的注意力图 {layer_idx: (B, H, L, L)}
        hidden_gradients: 各层的隐藏状态梯度 {layer_idx: (B, L)}
        method: 融合方法
            - 'gradcam': Grad-CAM 方式（注意力 × 梯度）
            - 'weighted_sum': 加权求和
            - 'multiply': 简单相乘
        layer_weights: 各层权重，None 表示均匀平均
    
    Returns:
        importance_scores: (B, L) - 每个样本、每个时间点的重要性得分
    """
    # 步骤 1: 从注意力矩阵提取对角线（自我关注度）
    # attn_diagonals[layer_idx] = (B, L)
    attn_diagonals = {}
    for layer_idx, attn in attention_maps.items():
        if attn.dim() == 4:  # (B, H, L, L)
            # 取所有头的对角线并平均
            B, H, L, _ = attn.shape
            diagonal = attn[:, :, range(L), range(L)]  # (B, H, L)
            attn_diagonals[layer_idx] = diagonal.mean(dim=1)  # (B, L)
        elif attn.dim() == 3:  # (B, L, L)
            B, L, _ = attn.shape
            attn_diagonals[layer_idx] = attn[:, range(L), range(L)]  # (B, L)
    
    # 步骤 2: 对每层进行注意力和梯度的融合
    fused_per_layer = []
    for layer_idx in attn_diagonals.keys():
        if layer_idx not in hidden_gradients:
            continue
            
        attn = attn_diagonals[layer_idx]  # (B, L)
        grad = hidden_gradients[layer_idx]  # (B, L)
        
        # 归一化到 [0, 1]
        attn_norm = normalize_for_fusion(attn)
        grad_norm = normalize_for_fusion(grad)
        
        # 融合
        if method == 'gradcam':
            fused = gradcam_fusion(attn_norm, grad_norm)
        elif method == 'weighted_sum':
            fused = weighted_sum_fusion(attn_norm, grad_norm, alpha=0.5)
        else:  # multiply
            fused = (attn_norm * grad_norm)
        
        fused_per_layer.append(fused)
    
    # 步骤 3: 对所有层的结果取平均（或加权平均）
    if len(fused_per_layer) == 0:
        return torch.zeros_like(list(attention_maps.values())[0][:, 0, :])  # (B, L)
    
    stacked = torch.stack(fused_per_layer, dim=0)  # (num_layers, B, L)
    
    if layer_weights is not None:
        # 加权平均
        weights = torch.tensor([layer_weights.get(i, 1.0) 
                               for i in range(len(fused_per_layer))],
                              dtype=torch.float32, device=stacked.device)
        weights = weights / weights.sum()
        importance = (stacked * weights[:, None, None]).sum(dim=0)
    else:
        # 均匀平均
        importance = stacked.mean(dim=0)  # (B, L)
    
    # 步骤 4: 最终归一化到 [0, 1]
    importance = normalize_for_fusion(importance)
    
    return importance
```

#### 2.2 在 Pipeline 中调用并可视化

**文件**: `pipeline.py` → `_run_visualization_stage()` 方法

```python
def _run_visualization_stage(
    self,
    results: Dict[str, Any],
    attention_maps: Dict[int, Tensor],
    output_dir: str,
) -> Dict[str, str]:
    """可视化输出阶段：生成热力图和统计图表。"""
    # ... 已有代码 ...
    
    # ========== 新增：时序数据融合可视化 ==========
    if self.config.skip_spatial:  # ECG/NLP/时序模型
        try:
            from .analyzer.fusion_utils import compute_token_importance
            from .visualization.timeseries_visualizer import TimeSeriesVisualizer
            
            # 1. 计算 token 重要性（融合注意力和梯度）
            token_importance = compute_token_importance(
                attention_maps=attention_maps,
                hidden_gradients=gradient_maps.get('hidden', {}),
                method='gradcam',  # 可配置
            )  # (B, L)
            
            # 2. 获取原始输入数据
            if hasattr(self, '_last_input_data'):
                sequence = self._last_input_data  # (C, L) 或 (L,)
                
                # 3. 创建可视化器（统一配置）
                ts_vis = TimeSeriesVisualizer(
                    sampling_rate=None,
                    time_unit='steps',
                )
                
                # 4. 生成融合可视化图
                fusion_path = os.path.join(output_dir, "token_importance_fusion.png")
                fig = ts_vis.render_sequence_with_attention(
                    sequence=sequence,
                    attention_weights=token_importance[0],  # 取第一个样本
                    channel_names=self._get_channel_names(),
                    title="Token Importance (Attention × Gradient Fusion)",
                    save_path=fusion_path,
                )
                saved_paths["token_importance_fusion"] = fusion_path
                logger.info("已生成融合可视化：％s", fusion_path)
                
                # 5. 同时保存纯注意力、纯梯度对比图
                # TODO: 可选实现
                
        except Exception as e:
            logger.error("时序数据融合可视化失败：%s", e)
```

#### 2.3 输出效果示例

**生成的新文件**: `token_importance_fusion.png`

**布局示例**（以 ECG 为例）:
```
┌──────────────────────────────────────────────┐
│ Token Importance (Attention × Gradient Fusion) │
├──────────────────────────────────────────────┤
│ Lead I                                        │
│  ╭─╮    ╭───╮                                │
│  │ │    │   │    ████                         │
│  │ ╰────╯   ╰────████  ██                     │
│  └────────────────────────────────────→ 时间   │
│      红色背景 = 高重要性区域                   │
├──────────────────────────────────────────────┤
│ Lead II                                       │
│    ╭─╮  ╭──╮                                 │
│    │ │  │  │    ████                          │
│    │ ╰──╯  ╰────████  ██                      │
│    └───────────────────────────────────→ 时间  │
└──────────────────────────────────────────────┘
```

**技术细节**:
- **X 轴**: 时间（秒）或 步数索引
- **Y 轴**: 信号幅度（mV）或 Embedding 值
- **背景颜色**: 红色深浅表示重要性高低（Reds colormap）
- **黑色折线**: 原始信号波形
- **透明度**: alpha=0.5（波形清晰可见）

---

### Step 2.4: 弃用 attention_heatmap.png

#### 问题分析

**当前问题**: `attention_heatmap.png` 只是单纯的注意力矩阵热力图，没有实际用途。

**原因**:
1. **维度不匹配**: 注意力矩阵是 `(L, L)` 的方阵（如 125×125），无法直接对应到原始序列的 2500 个时间点
2. **缺少梯度信息**: 只反映注意力分布，不包含梯度贡献度
3. **难以解读**: 对角线外的区域含义不明确（Query 和 Key 的关系抽象）
4. **与原始数据无映射**: 无法回答"模型关注波形的哪个位置"这个核心问题

**对比**:
| 图表 | 内容 | 能否对应原始数据 | 是否包含梯度 | 实际用途 |
|------|------|-----------------|-------------|---------|
| `attention_heatmap.png` | 注意力矩阵 (125×125) | ❌ 不能 | ❌ 否 | ❌ 调试用途 |
| `token_importance_fusion.png` | 融合重要性 (2500 点) | ✅ 能 | ✅ 是 | ✅ 主要输出 |

---

#### 修改方案

**文件**: `pipeline.py` → `_run_visualization_stage()`

```python
def _run_visualization_stage(
    self,
    results: Dict[str, Any],
    attention_maps: Dict[int, Tensor],
    output_dir: str,
) -> Dict[str, str]:
    """可视化输出阶段：生成热力图和统计图表。"""
    # ... 已有代码 ...
    
    # HeatmapRenderer - 生成所有头的全景热力图
    if self._heatmap_renderer is not None:
        try:
            # 1. 所有层所有头的热力图面板（每行 6 个）
            if attention_maps:
                all_heads_path = os.path.join(output_dir, "all_heads_attention_heatmap.png")
                self._heatmap_renderer.render_multihead_all_heads(
                    attention_maps=attention_maps,
                    title="All Attention Heads Heatmap (6 columns)",
                    save_path=all_heads_path,
                    num_cols=6,
                )
                saved_paths["all_heads_heatmap"] = all_heads_path
                logger.info("已生成所有头的热力图：％s", all_heads_path)
            
            # ❌【已弃用】传统的单头热力图（无实际意义）
            # attn_path = os.path.join(output_dir, "attention_heatmap.png")
            # if attention_maps:
            #     first_attn = next(iter(attention_maps.values()))
            #     self._heatmap_renderer.render_attention(
            #         first_attn[0, 0], title="...", save_path=attn_path
            #     )
            #     saved_paths["attention_heatmap"] = attn_path
            #     logger.info("已生成单头热力图：％s", attn_path)
            
            # ✅ 改用新的融合可视化（见 Step 2.2）
            # token_importance_fusion.png 将替代 attention_heatmap.png
            
            # 2. 多层对比面板
            if len(attention_maps) > 1:
                multi_layer_path = os.path.join(output_dir, "multi_layer_attention.png")
                self._heatmap_renderer.render_multi_layer(
                    layer_maps=attention_maps,
                    title="Multi-Layer Attention Comparison",
                    save_path=multi_layer_path,
                    num_cols=4,
                )
                saved_paths["multi_layer_comparison"] = multi_layer_path
                logger.info("已生成多层对比图：％s", multi_layer_path)
                
        except Exception as e:
            logger.error("热力图渲染失败：%s", e)
```

---

#### 输出文件变化

##### 改进前（8 个文件）
```
analysis_output/
├── all_heads_attention_heatmap.png       # 所有头全景（保留）
├── attention_heatmap.png                 # ❌ 单头热力图（弃用）
├── multi_layer_attention.png             # 多层对比（保留）
├── layer_importance.png                  # 层重要性折线图（保留）
├── head_frequency_importance_scatter.png # 散点图（保留）
├── accumulator_stats.png                 # 累积器统计（保留）
└── analysis_report.md                    # 综合报告（保留）
```

##### 改进后（7 个文件 + 可选扩展）
```
analysis_output/
├── all_heads_attention_heatmap.png       # 所有头全景（保留）
├── token_importance_fusion.png           # ✅ NEW! 融合可视化 ⭐ 主要图表
├── multi_layer_attention.png             # 多层对比（保留）
├── layer_importance.png                  # 层重要性折线图（保留）
├── head_frequency_importance_scatter.png # 散点图（保留）
├── accumulator_stats.png                 # 累积器统计（保留）
├── analysis_report.md                    # 综合报告（保留）
└── key_segments_annotation.png           # ✅ NEW! 关键位置标注（可选）
```

**变化说明**:
- **删除**: `attention_heatmap.png` （无实际用途）
- **新增**: `token_importance_fusion.png` （注意力×梯度融合，对应原始数据）
- **新增**: `key_segments_annotation.png` （关键位置标注，可选）
- **净变化**: 8 个 → 7-9 个（取决于是否启用领域专用标注）

如果用户之前依赖 `attention_heatmap.png` 进行调试，可以：
不允许用户再使用`attention_heatmap.png`作为输出之一。


---
### Step 3: 实现通用峰值检测功能

#### 设计理念

**文件**: `visualization/timeseries_visualizer.py`

**核心原则**:
1. **纯通用方法**: 只使用统计学方法检测峰值、谷值、突变点
2. **无领域知识**: 不依赖任何特定领域的先验规则
3. **内置静态方法**: 直接在 `TimeSeriesVisualizer` 中提供
4. **参考模式**: 参考 scipy.signal.find_peaks 的通用设计

---

#### 3.1 完整实现代码

```
"""
时序数据通用可视化工具

支持任何 1D 序列数据的注意力叠加和关键点标注。

设计原则：
- 通用方法：只使用统计学检测，不依赖领域知识
- 职责单一：只做可视化和基础信号处理
- 易于使用：开箱即用，无需配置

使用示例：
    ts_vis = TimeSeriesVisualizer(sampling_rate=500)
    
    # 渲染注意力叠加图
    fig = ts_vis.render_sequence_with_attention(
        sequence=data,              # (L,) 或 (C, L)
        attention_weights=importance,  # (L,)
        save_path="output.png",
    )
    
    # 自动检测并标注峰值
    key_positions = ts_vis.detect_key_positions(data, method='peaks')
    # key_positions = {
    #     "peaks": [(start, end), ...],
    #     "valleys": [(start, end), ...],
    # }
"""

from typing import Dict, List, Optional, Tuple
import torch
from torch import Tensor
import numpy as np
import matplotlib.pyplot as plt
from matplotlib.figure import Figure
from matplotlib.axes import Axes
from scipy.signal import find_peaks


class TimeSeriesVisualizer:
    """时序数据通用可视化器"""
    
    def __init__(
        self,
        sampling_rate: Optional[float] = None,
        time_unit: str = "steps",
    ):
        """
        Args:
            sampling_rate: 采样率 (Hz)，None 表示用时间步索引
            time_unit: X 轴单位 ('seconds', 'milliseconds', 'steps')
        """
        self.sampling_rate = sampling_rate
        self.time_unit = time_unit
    
    @staticmethod
    def detect_peaks(
        signal: np.ndarray,
        distance: int = 50,
        prominence: float = 0.1,
    ) -> List[Tuple[int, int]]:
        """
        检测序列中的峰值区域（通用方法）
        
        Args:
            signal: 输入信号 (L,)
            distance: 峰值间最小距离（点数）
            prominence: 峰值突出度阈值
        
        Returns:
            [(start_idx, end_idx), ...] - 每个峰值区域的起止索引
        """
        peaks, properties = find_peaks(signal, distance=distance, prominence=prominence)
        
        peak_regions = []
        for peak in peaks:
            # 峰值前后各扩展 10 个点作为区域
            start = max(0, peak - 10)
            end = min(len(signal), peak + 10)
            peak_regions.append((start, end))
        
        return peak_regions
    
    @staticmethod
    def detect_valleys(
        signal: np.ndarray,
        distance: int = 50,
        prominence: float = 0.1,
    ) -> List[Tuple[int, int]]:
        """检测谷值区域（对负信号使用 find_peaks）"""
        return TimeSeriesVisualizer.detect_peaks(-signal, distance, prominence)
    
    @staticmethod
    def detect_change_points(
        signal: np.ndarray,
        threshold_factor: float = 2.0,
    ) -> List[Tuple[int, int]]:
        """
        检测序列中的突变点（通用方法）
        
        Args:
            signal: 输入信号 (L,)
            threshold_factor: 差分阈值因子（均值 + factor*标准差）
        
        Returns:
            [(start_idx, end_idx), ...] - 每个突变点的邻域
        """
        diff = np.abs(np.diff(signal))
        threshold = np.mean(diff) + threshold_factor * np.std(diff)
        change_points = np.where(diff > threshold)[0]
        
        change_regions = []
        for cp in change_points:
            start = max(0, cp - 5)
            end = min(len(signal), cp + 5)
            change_regions.append((start, end))
        
        return change_regions
    
    def detect_key_positions(
        self,
        signal: np.ndarray,
        method: str = "auto",
    ) -> Dict[str, List[Tuple[int, int]]]:
        """
        自动检测关键位置（通用方法）
        
        Args:
            signal: 输入信号 (L,)
            method: 检测方法
                - 'peaks': 只检测峰值
                - 'valleys': 只检测谷值
                - 'changes': 只检测突变点
                - 'all': 检测所有类型
                - 'auto': 自动选择（默认同'all'）
        
        Returns:
            {
                "peaks": [(start, end), ...],
                "valleys": [(start, end), ...],
                "change_points": [(start, end), ...],
            }
        """
        result = {}
        
        if method in ["peaks", "all", "auto"]:
            result["peaks"] = self.detect_peaks(signal)
        
        if method in ["valleys", "all", "auto"]:
            result["valleys"] = self.detect_valleys(signal)
        
        if method in ["changes", "all", "auto"]:
            result["change_points"] = self.detect_change_points(signal)
        
        return result
```

---

#### 3.2 在 Pipeline 中调用

**文件**: `pipeline.py` → `_run_visualization_stage()`

```python
if self.config.skip_spatial:  # 时序数据
    try:
        from .visualization.timeseries_visualizer import TimeSeriesVisualizer
        
        # ========== 步骤 1: 计算 token 重要性 ==========
        token_importance = compute_token_importance(...)
        
        # ========== 步骤 2: 检测关键位置（通用方法） ==========
        sequence = self._last_input_data.cpu().numpy()
        
        # 所有数据类型统一使用通用峰值检测
        ts_vis = TimeSeriesVisualizer()
        key_positions = ts_vis.detect_key_positions(
            sequence[0],  # 取第一个通道
            method='auto'  # 自动检测峰值、谷值、突变点
        )
        
        # ========== 步骤 3: 渲染标注图 ==========
        annotation_path = os.path.join(output_dir, "key_segments_annotation.png")
        fig = ts_vis.render_sequence_with_attention(
            sequence=self._last_input_data,
            attention_weights=token_importance[0],
            key_positions=key_positions,
            save_path=annotation_path,
        )
        saved_paths["key_segments_annotation"] = annotation_path
        logger.info("已生成关键区段标注：%s", annotation_path)
        
    except Exception as e:
        logger.error("关键区段标注失败：%s", e)
```

---

#### 详细实现代码

**文件**: `visualization/timeseries_visualizer.py`

```python
class TimeSeriesVisualizer:
    """时序数据通用可视化器"""
    
    def __init__(
        self,
        sampling_rate: Optional[float] = None,
        time_unit: str = "steps",
    ):
        """
        Args:
            sampling_rate: 采样率 (Hz)，None 表示用时间步索引
            time_unit: X 轴单位 ('seconds', 'milliseconds', 'steps')
        """
        self.sampling_rate = sampling_rate
        self.time_unit = time_unit
    
    @staticmethod
    def detect_peaks(
        signal: np.ndarray,
        distance: int = 50,
        prominence: float = 0.1,
    ) -> List[Tuple[int, int]]:
        """
        检测序列中的峰值区域（通用方法）
        
        Args:
            signal: 输入信号 (L,)
            distance: 峰值间最小距离（点数）
            prominence: 峰值突出度阈值
        
        Returns:
            [(start_idx, end_idx), ...] - 每个峰值区域的起止索引
        """
        peaks, properties = find_peaks(signal, distance=distance, prominence=prominence)
        
        peak_regions = []
        for peak in peaks:
            # 峰值前后各扩展 10 个点作为区域
            start = max(0, peak - 10)
            end = min(len(signal), peak + 10)
            peak_regions.append((start, end))
        
        return peak_regions
    
    @staticmethod
    def detect_valleys(
        signal: np.ndarray,
        distance: int = 50,
        prominence: float = 0.1,
    ) -> List[Tuple[int, int]]:
        """检测谷值区域（对负信号使用 find_peaks）"""
        return TimeSeriesVisualizer.detect_peaks(-signal, distance, prominence)
    
    @staticmethod
    def detect_change_points(
        signal: np.ndarray,
        threshold_factor: float = 2.0,
    ) -> List[Tuple[int, int]]:
        """
        检测序列中的突变点（通用方法）
        
        Args:
            signal: 输入信号 (L,)
            threshold_factor: 差分阈值因子（均值 + factor*标准差）
        
        Returns:
            [(start_idx, end_idx), ...] - 每个突变点的邻域
        """
        diff = np.abs(np.diff(signal))
        threshold = np.mean(diff) + threshold_factor * np.std(diff)
        change_points = np.where(diff > threshold)[0]
        
        change_regions = []
        for cp in change_points:
            start = max(0, cp - 5)
            end = min(len(signal), cp + 5)
            change_regions.append((start, end))
        
        return change_regions
    
    def detect_key_positions(
        self,
        signal: np.ndarray,
        method: str = "auto",
    ) -> Dict[str, List[Tuple[int, int]]]:
        """
        自动检测关键位置（通用方法）
        
        Args:
            signal: 输入信号 (L,)
            method: 检测方法
                - 'peaks': 只检测峰值
                - 'valleys': 只检测谷值
                - 'changes': 只检测突变点
                - 'all': 检测所有类型
                - 'auto': 自动选择（默认同'all'）
        
        Returns:
            {
                "peaks": [(start, end), ...],
                "valleys": [(start, end), ...],
                "change_points": [(start, end), ...],
            }
        """
        result = {}
        
        if method in ["peaks", "all", "auto"]:
            result["peaks"] = self.detect_peaks(signal)
        
        if method in ["valleys", "all", "auto"]:
            result["valleys"] = self.detect_valleys(signal)
        
        if method in ["changes", "all", "auto"]:
            result["change_points"] = self.detect_change_points(signal)
        
        return result
    
    def render_sequence_with_attention(
        self,
        sequence: Tensor,           # (C, L) 多通道或 (L,) 单通道
        attention_weights: Tensor,  # (L,) 或 (H, L) - 每时间点的重要性
        gradient_weights: Optional[Tensor] = None,  # (L,) - 梯度重要性 (可选)
        fusion_method: str = "gradcam",  # 'gradcam' | 'sum' | 'multiply'
        key_positions: Optional[Dict[str, List[Tuple[int, int]]]] = None,
        channel_names: List[str] = None,  # ['Lead I', 'Lead II'] 或 ['Embed_0', ...]
        title: str = None,
        save_path: str = None,
        figsize: Tuple[int, int] = (14, 8),
    ) -> Figure:
        """
        渲染时序数据 + 注意力热力图叠加
        
        支持多通道分别显示，每行一个通道
        
        Args:
            sequence: 原始输入序列
            attention_weights: 注意力权重（已融合为每时间点的标量）
            gradient_weights: 梯度权重（可选，用于 Grad-CAM 融合）
            fusion_method: 注意力和梯度的融合方式
            key_positions: 关键位置字典（由外部检测器提供）
                {
                    "P_waves": [(start, end), ...],
                    "QRS_complexes": [(start, end), ...],
                }
            channel_names: 通道名称列表
            title: 图表标题
            save_path: 保存路径
            figsize: 图像尺寸
        
        Returns:
            Figure 对象
        """
        # 1. 处理序列数据
        if sequence.dim() == 2:
            sequences = [sequence[i] for i in range(sequence.shape[0])]
            if channel_names is None:
                channel_names = [f"Channel_{i}" for i in range(len(sequences))]
        else:
            sequences = [sequence]
            if channel_names is None:
                channel_names = ["Input Sequence"]
        
        # 2. 创建多子图
        fig, axes = plt.subplots(
            nrows=len(sequences),
            ncols=1,
            figsize=figsize,
            sharex=True,
            dpi=150,
        )
        if len(sequences) == 1:
            axes = [axes]
        
        # 3. 对每个通道绘图
        for ax, seq, name in zip(axes, sequences, channel_names):
            # 绘制原始波形
            x = np.arange(len(seq))
            ax.plot(x, seq.cpu().numpy(), color='black', linewidth=0.8, label=name)
            
            # 叠加注意力热力图背景
            self._overlay_attention(ax, attention_weights, seq.shape[0])
            
            # 标注关键区段（如果有提供）
            if key_positions is not None:
                self.annotate_key_segments(
                    ax,
                    key_positions,
                    attention_weights,
                    highlight_color="yellow",
                    alpha=0.3,
                )
            
            # 设置坐标轴
            ax.set_ylabel("Amplitude")
            ax.legend(loc='upper right')
            ax.grid(True, alpha=0.3)
        
        # X 轴标签和标题
        if self.sampling_rate is not None:
            time_axis = x / self.sampling_rate
            axes[-1].set_xticks(time_axis[::int(self.sampling_rate)])
            axes[-1].set_xticklabels([f"{t:.1f}" for t in time_axis[::int(self.sampling_rate)]])
            axes[-1].set_xlabel("Time (seconds)")
        else:
            axes[-1].set_xlabel("Time Steps")
        
        fig.suptitle(title or "Sequence Attention Overlay", fontsize=14, y=1.02)
        plt.tight_layout()
        
        # 保存或返回
        if save_path:
            os.makedirs(os.path.dirname(save_path), exist_ok=True)
            fig.savefig(save_path, dpi=150, bbox_inches='tight')
            plt.close(fig)
        else:
            return fig
    
    def _overlay_attention(
        self,
        ax: Axes,
        attention_weights: Tensor,
        num_points: int,
        cmap_name: str = "Reds",
        alpha: float = 0.5,
    ):
        """
        在轴上叠加注意力热力图背景
        
        Args:
            ax: matplotlib 轴对象
            attention_weights: 注意力权重 (L,)
            num_points: 序列长度
            cmap_name: 颜色映射
            alpha: 透明度
        """
        # 归一化到 [0, 1]
        attn = attention_weights.cpu().numpy()
        attn = (attn - attn.min()) / (attn.max() - attn.min() + 1e-8)
        
        # 如果序列长度不匹配，进行插值
        if len(attn) != num_points:
            from scipy.interpolate import interp1d
            f = interp1d(np.linspace(0, 1, len(attn)), attn, kind='linear')
            attn = f(np.linspace(0, 1, num_points))
        
        # 逐段绘制颜色背景
        cmap = plt.get_cmap(cmap_name)
        for i in range(num_points - 1):
            ax.axvspan(i, i+1, 
                      facecolor=cmap(attn[i]), 
                      alpha=alpha,
                      linewidth=0)
    
    def annotate_key_segments(
        self,
        ax: Axes,
        key_positions: Dict[str, List[Tuple[int, int]]],
        attention_weights: Tensor,
        highlight_color: str = "yellow",
        alpha: float = 0.3,
        min_importance: float = 0.3,
    ):
        """
        在已有轴上标注关键区段
        
        Args:
            ax: matplotlib 轴对象
            key_positions: 关键位置字典（由外部提供）
            attention_weights: 注意力权重（用于筛选重要区段）
            highlight_color: 高亮颜色
            alpha: 透明度
            min_importance: 最小重要性阈值，低于此值不标注
        """
        for segment_type, positions in key_positions.items():
            for start_idx, end_idx in positions:
                # 确保索引在有效范围内
                start_idx = max(0, min(start_idx, len(attention_weights)-1))
                end_idx = max(start_idx+1, min(end_idx, len(attention_weights)))
                
                # 计算该区段的平均重要性
                seg_importance = attention_weights[start_idx:end_idx].mean().item()
                
                # 只标注重要性较高的区段
                if seg_importance > min_importance:
                    # 添加高亮背景
                    ax.axvspan(start_idx, end_idx, 
                              facecolor=highlight_color, 
                              alpha=alpha,
                              linewidth=0)
                    
                    # 添加标注文字
                    ax.annotate(
                        f"{segment_type}\n(imp={seg_importance:.2f})",
                        xy=((start_idx + end_idx) / 2, 0),
                        xytext=(0, 10),
                        textcoords='offset points',
                        ha='center',
                        va='bottom',
                        fontsize=8,
                        bbox=dict(boxstyle='round,pad=0.3', 
                                 facecolor='white', 
                                 edgecolor=highlight_color, 
                                 alpha=0.8),
                    )
    
    def render_multi_layer_comparison(
        self,
        layer_sequences: Dict[int, Tensor],  # {layer_idx: (L,)}
        sequence: Tensor,
        save_path: str = None,
    ) -> Figure:
        """
        多层注意力对比图
        
        每行显示一个 Transformer 层的注意力分布
        """
        # ... 实现 ...
```

---

### Step 4: 修改 Pipeline 调用逻辑

**文件**: `pipeline.py` → `_run_visualization_stage()`

```
# 新增时序数据可视化分支（通用处理，不区分数据类型）
if self.config.skip_spatial:  # 时序数据
    try:
        from .visualization.timeseries_visualizer import TimeSeriesVisualizer
        
        # 1. 创建通用可视化器（统一配置，不判断数据类型）
        ts_vis = TimeSeriesVisualizer(
            sampling_rate=None,  # 默认用时间步索引
            time_unit='steps',
        )
        
        # 2. 从 attention_maps 提取 token 重要性
        token_importance = MetricsCalculator.compute_token_importance(
            attention_maps=attention_maps,
            hidden_gradients=gradient_maps.get('hidden', {}),
        )
        
        # 3. 生成叠加图（通用）
        if hasattr(self, '_last_input_data'):
            fig = ts_vis.render_sequence_with_attention(
                sequence=self._last_input_data,
                attention_weights=token_importance,
                channel_names=self._get_channel_names(),
                title="Attention Weights Overlaid on Input Sequence",
                save_path=os.path.join(output_dir, "sequence_attention_overlay.png"),
            )
            saved_paths["sequence_attention_overlay"] = ...
            
        # 4. 生成多层对比图
        fig2 = ts_vis.render_multi_layer_comparison(
            layer_sequences={
                layer_idx: attn.mean(dim=(0,1))  # 平均所有头
                for layer_idx, attn in attention_maps.items()
            },
            sequence=self._last_input_data,
            save_path=os.path.join(output_dir, "multi_layer_attention.png"),
        )
        saved_paths["multi_layer_comparison"] = ...
        
        # 5. 关键位置标注（使用通用峰值检测）
        sequence_np = self._last_input_data.cpu().numpy()
        key_positions = ts_vis.detect_key_positions(
            sequence_np[0],  # 取第一个通道
            method='auto'    # 自动检测峰值、谷值、突变点
        )
        
        fig3 = ts_vis.render_sequence_with_attention(
            sequence=self._last_input_data,
            attention_weights=token_importance,
            key_positions=key_positions,  # 传入检测结果
            title="Key Segments Annotation",
            save_path=os.path.join(output_dir, "key_segments_annotation.png"),
        )
        saved_paths["key_segments_annotation"] = ...
        
    except Exception as e:
        logger.error("时序可视化失败：%s", e)
else:  # 图像模型 (ViT, Swin)
    # 原有的空间重构 + 热力图逻辑
    ...
```

---

## 📊 输出文件变化

### 改进前（5-8 个文件）
```
- all_heads_attention_heatmap.png
- attention_heatmap.png          ← 单纯注意力矩阵，无实际意义
- multi_layer_attention.png
- layer_importance.png
- head_frequency_importance_scatter.png
- accumulator_stats.png
- analysis_report.md
```

### 改进后（7-10 个文件，通用命名）
```
- all_heads_attention_heatmap.png
- sequence_attention_overlay.png  ← NEW! 主要图表 ⭐（通用名称）
- multi_layer_attention.png       ← 改进！多层对比
- key_segments_annotation.png     ← NEW! 关键位置标注
- layer_importance.png
- head_frequency_importance_scatter.png
- accumulator_stats.png
- analysis_report.md

**说明**: 不再提供领域专用图表，所有数据类型统一输出相同的图表格式。
```

---

## 🔧 修改清单

**需要新增的文件**:
- [x] `visualization/timeseries_visualizer.py` - **时序数据通用可视化类** ⭐

**需要修改的文件**:
- [x] `pipeline.py` - 添加时序数据可视化分支（通用逻辑）
- [x] `analyzer/fusion_utils.py` - 实现 `compute_token_importance()`
- [x] `visualization/__init__.py` - 导出 TimeSeriesVisualizer

### 需要弃用的文件
- [x] `attention_heatmap.png` - 不再作为主要输出（改为调试用途）

---

## 🎨 可视化设计规范

### 图表 1: 时序信号 + 注意力叠加图（通用）

**布局**:
```
┌─────────────────────────────────────┐
│ Channel 0: [波形 + 红色背景热力图]   │
├─────────────────────────────────────┤
│ Channel 1: [波形 + 红色背景热力图]   │
├─────────────────────────────────────┤
│ Channel 2: [波形 + 红色背景热力图]   │
└─────────────────────────────────────┘
```

**技术细节**:
- X 轴：时间（秒）或 步数/Token 索引
- Y 轴：幅度（mV）或 Embedding 值
- 热力图颜色：Reds colormap（越红越重要）
- 透明度：alpha=0.5
- 标注：在顶部用箭头标出高注意力区域 → "高注意力区 (0.8-1.2 秒)"

---

### 图表 2: 多层注意力对比

**布局**:
```
Layer 0:  [▁▂▃▅▆▇█───────]
Layer 1:  [▁▂▃▅▆▇█───────]
Layer 2:  [▁▂▃▅▆▇█───────]
...
Layer 5:  [▁▂▅▆▇█───────]
```

**目的**: 对比不同 Transformer 层的关注模式差异
- 浅层可能关注低层次特征（QRS 波、词性）
- 深层可能关注高层次模式（节律异常、语义）

---

### 图表 3: 关键位置标注

**示例（波）**:
```
幅度 (mV)
  ↑
  │         ┌─ 重点1─┐    ┌── 重点2──┐     ┌─ 重点3 ─
  │         │  ★★★  │    │  █████  │     │  ★★★  │
  │    ○────┘       └────┘         └─────       └────○
  │
  └────────────────────────────────────────────→ 时间 (秒)
       0    1    2    3    4    5
       
       ★ = 中等注意力  █ = 高注意力
```

**示例（NLP）**:
```
"The quick brown fox jumps over the lazy dog"
      ████          ███████                  ██
      adj+noun      verb                     art+noun
      
      █ = 高注意力 (动词短语)  ▓ = 中等注意力 (名词短语)
```

---

## ⏱️ 实施时间表

| 阶段 | 任务 | 预计时间 |
|------|------|---------|
| **Phase 1** | 实现 `TimeSeriesVisualizer` 基础类 | 2 小时 |
| **Phase 2** | 实现 `compute_token_importance()` | 1 小时 |
| **Phase 3** | 实现通用特征检测器 (`generic_detector`) | 1.5 小时 |
| **Phase 4** | 修改 `pipeline.py` 集成 | 1.5 小时 |
| **Phase 5** | 实现领域专用检测器 (ECG/NLP，可选) | 2 小时 |
| **Phase 6** | 测试与调试（ECG + NLP 双场景） | 2 小时 |
| **总计** | | **10 小时** |

---

## ✅ 验收标准

### 功能验收（通用时序）
1. ✅ 能够生成清晰的**序列 + 注意力叠加图**
2. ✅ 支持多通道显示（如 ECG 多导联、NLP 多 embedding 维度）
3. ✅ X 轴标注合理（时间/步数/token 索引）
4. ✅ 高注意力区域有明显颜色标注
5. ✅ 报告文件中说明"模型最关注的时间步位于 X-X"

### 功能验收（领域专用）
- **ECG**:
  - [ ] 至少显示 2 个导联（Lead I, Lead II）
  - [ ] X 轴标注时间（秒），范围 0-5 秒
  - [ ] 自动检测并标注 QRS 波群
  
- **NLP**:
  - [ ] 显示每个 token 的文本（英文单词/中文分词）
  - [ ] 标注关键词/短语边界
  
- **金融**:
  - [ ] 标注峰值/谷值
  - [ ] 显示日期/时间轴

### 质量验收
1. ✅ 图像分辨率 ≥ 150 DPI
2. ✅ 序列清晰可见，不被热力图完全覆盖
3. ✅ 颜色映射专业（推荐 Reds/YlOrRd）
4. ✅ 多图布局整洁，标题完整
5. ✅ **代码复用性高**：同一套代码支持 ECG/NLP/金融等多种数据

---

## 🚀 后续扩展

### Phase 2: 临床/领域解释增强
- [ ] **ECG**: 自动识别心律失常类型，标注异常波段
- [ ] **NLP**: 句法树可视化，依存关系标注
- [ ] **金融**: 技术指标叠加（MA, MACD, RSI）

### Phase 3: 批量分析
- [ ] 统计 100 个样本的平均注意力模式
- [ ] 比较不同类别的注意力差异
- [ ] 生成群体级别的统计报告

---

## 🎓 参考实现

查看 `tests/test_model/attention_heatmap.py` 第 88-114 行，那里有**正确的波形 + 热力图叠加示例**！

**关键代码片段**:
```
# 1. 从注意力矩阵提取 token 重要性
token_importance = attention_matrix.mean(axis=0)
token_importance = normalize(token_importance)

# 2. 上采样到信号长度
attention_upsampled = np.repeat(token_importance, 8)  # 假设 8 倍上采样

# 3. 绘制波形 + 颜色背景
for t in range(len(signal)):
    axes.axvspan(time_axis[t], time_axis[t+1], 
                 color=cmap(attention_upsampled[t]), alpha=0.6)
axes.plot(signal, color='black')
```

**我们的任务是将这段代码封装为可复用的通用类！**

---

*创建时间：2026-04-02*  
*版本：v1.0 - 通用时序数据设计*  
*负责人：Transformer Analyzer Team*

---

## 📊 实施状态（已完成）

**完成时间**: 2026-04-02  
**状态**: ✅ **全部完成**

### 已完成的任务

1. ✅ **Step 1**: 创建 `TimeSeriesVisualizer` 类（404 行代码）
   - 峰值/谷值/突变点检测
   - 序列 + 注意力叠加渲染
   - 关键区段标注
   - 多层对比图

2. ✅ **Step 2**: 实现 `compute_token_importance()` 函数
   - Grad-CAM 融合
   - 加权求和融合
   - 相乘融合
   - 自定义层权重

3. ✅ **Step 3**: 集成到 Pipeline
   - 保存输入数据
   - 自动检测时序数据
   - 生成 3 种新图表
   - 弃用旧版单头热力图

4. ✅ **Step 4**: 完整测试
   - 单元测试通过
   - 生成测试图像
   - 编写实现报告

### 输出文件清单

**新增文件**:
- `visualization/timeseries_visualizer.py` (404 行)
- `tests/test_timeseries_visualization.py` (266 行)
- `时序数据可视化增强实现报告.md` (391 行)

**修改文件**:
- `pipeline.py` (+99 行)
- `analyzer/fusion_utils.py` (+84 行)
- `visualization/__init__.py` (+2 行)
- `时序数据可视化增强计划.md` (更新任务状态)

**生成的可视化文件**（每个样本）:
- `token_importance_fusion.png` ⭐ 主要图表
- `key_segments_annotation.png`
- `multi_layer_attention_seq.png`
- `all_heads_attention_heatmap.png` (保留)
- `multi_layer_attention.png` (保留)

**已弃用**:
- `attention_heatmap.png` (已注释)

### 测试结果

```
✅ TimeSeriesVisualizer 所有测试通过
✅ compute_token_importance 所有测试通过
⚠️ Pipeline 集成测试跳过（缺少测试模型加载接口）
```

**测试图像**:
- `test_output/test_sequence_overlay.png` (271.6KB)
- `test_output/test_sequence_annotated.png` (274.9KB)
- `test_output/test_multi_layer.png` (117.7KB)

### 实际用时

| 阶段 | 预计时间 | 实际时间 |
|------|---------|---------|
| 实现 TimeSeriesVisualizer | 2 小时 | 1.5 小时 |
| 实现 compute_token_importance | 1 小时 | 0.5 小时 |
| 集成到 Pipeline | 1.5 小时 | 1 小时 |
| 测试与调试 | 2 小时 | 1 小时 |
| **总计** | **6.5 小时** | **4 小时** |

### 下一步建议

- [ ] 批量分析功能（统计 100 个样本的平均模式）
- [ ] 领域专用检测器（按需实现，如 ECG 的 P/QRS/T 波识别）
- [ ] 交互式可视化（Plotly/Dash 支持）
- [ ] 实时流式数据处理

---
